name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Verify gradients (finite differences)
        run: python gradcheck.py
      - name: Train (smoke test)
        run: python train.py
      - name: Language model (smoke test)
        run: python train_lm.py --steps 3 --batch 4 --hidden 16 --sample-every 1 --sample-len 20

  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install security tools
        run: python -m pip install --upgrade pip bandit pip-audit
      - name: Static analysis (bandit)
        run: bandit -r . --ini .bandit
      - name: Dependency CVE scan (pip-audit)
        run: pip-audit -r requirements.txt
